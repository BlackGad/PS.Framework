<ContentControl x:Class="PS.Commander.Views.ExplorerView"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                xmlns:viewModels="clr-namespace:PS.Commander.ViewModels"
                d:DataContext="{d:DesignInstance viewModels:ExplorerViewModel}"
                d:DesignHeight="450"
                d:DesignWidth="800"
                Focusable="False"
                mc:Ignorable="d">
    <ListView ItemsSource="{Binding Items}">
        <ListView.View>
            <GridView AllowsColumnReorder="true" ColumnHeaderToolTip="File Information">
                <GridViewColumn DisplayMemberBinding="{Binding Name}" Header="Name" />
                <GridViewColumn Width="100"
                                DisplayMemberBinding="{Binding Extension}"
                                Header="Ext" />
            </GridView>
        </ListView.View>
    </ListView>
</ContentControl>